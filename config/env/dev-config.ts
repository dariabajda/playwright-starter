import { CustomTestConfig } from '../custom-config';

export const devConfig: CustomTestConfig = {
  bookingApiUrl: 'https://restful-booker.herokuapp.com',
  baseUrl: 'https://dev.example.com',
  apiUrl: 'https://dev.api.example.com',
};
